---
description: Sync changes to GitHub
---

# Sync with GitHub

1. Check the current status
// turbo
2. Stage all changes
// turbo
3. Commit with a timestamped message
// turbo
4. Push to remote

```bash
git status
git add .
git commit -m "Update: $(date)"
git push origin master
```
